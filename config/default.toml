# PopSub default configuration
[server]
host = "127.0.0.1"
port = 8080
jwt_secret = "a_very_secret_key"
jwt_expiration_hours = 24
username = "admin"
password = "password"

[broker]
max_connections = 1000
message_ttl_secs = 3600
ack_timeout_ms = 5000
max_retries = 5
